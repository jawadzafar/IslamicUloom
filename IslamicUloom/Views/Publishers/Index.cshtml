@model IEnumerable<IslamicUloom.Models.Publisher>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PublisherName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PublisherAddress)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PublisherName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PublisherAddress)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PublisherId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PublisherId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PublisherId })
        </td>
    </tr>
}

</table>
